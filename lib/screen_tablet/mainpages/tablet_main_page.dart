import 'package:flutter/material.dart';

class Tablet_MainPage extends StatelessWidget {
  const Tablet_MainPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        appBar: AppBar(),
      ),
    );
  }
}
